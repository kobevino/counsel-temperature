# 대화 온도 계산식

현재 코드 기준 요약. 출처는 `lib/score.ts`, `lib/signals.ts`, `lib/clock.ts`, `lib/time.ts`, `app/api/temperature/route.ts`, `lib/intervention.ts`.

## 한 줄 계산식

```
온도 = round( clamp₀₋₁₀₀( 0.40·intent + 0.25·engagement + 0.20·trust + 0.15·(100 − resistance) ) )
```

`lib/score.ts:31`. 높을수록 따뜻한(전환에 가까운) 상담이다 — v1의 "이탈 위험도"와 방향이 반대이므로 두 수치를 섞지 말 것.

## 축 점수는 어떻게 나오나

**누적식이 아니라 매 턴 전체 재계산이다.** 이전 온도에 Δ를 더하는 게 아니라, 매번 초기값에서 대화 전체의 신호를 다시 쌓는다 (`app/api/temperature/route.ts:130`).

```
axes = clamp₀₋₁₀₀( baseline + Σ(감지된 신호의 Δ) )   ← 신호 하나 적용할 때마다 clamp
```

### baseline (`lib/score.ts:17`)

| | intent | engagement | trust | resistance | → 온도 |
|---|---|---|---|---|---|
| 기본 `INITIAL_AXES` | 45 | 60 | 50 | 20 | **55** |
| `WARY_AXES` (첫 발화부터 짜증·의심 톤) | 45 | 60 | 35 | 35 | **45** |

### 신호 Δ 테이블 (`lib/signals.ts:29`)

발화 신호는 LLM(또는 mock 감지기)이 "무슨 신호가 있었나"만 고르고, 산술은 전부 코드가 한다. 주요 항목:

- **intent** (가중 0.40) — 가입 의사 +35 / 시점 언급 +25 / 절차 문의 +20 / 가족 추가 문의 +15 / 일반 문의 +8 / 가격 부담 −8·저항 +25 / 다른 상품 요구 −12 / 보류 −30·저항 +30 / 거절 −45·저항 +15
- **engagement** (0.25) — 자발적 화제 +10 / 구체 조건 질문 +8 / 최소 응답 −15 / 발화 길이 절반 이하 −12 / 화제 전환 −10·저항 +15
- **trust** (0.20) — 자발 공개 +18 / 즉답 받음 +12 / 수집 목적 설명 +10 / 목적 없는 정보 요구 −15 / 이미 준 정보 재요구 −15 / 정보 거부 −20·저항 +20 / 강요 의심 −25 / 응대 확인 재촉 −10
- **resistance** (0.15 · 역산) — 대안 수용 −30 / 긍정 표현 −20 / 우려 해소 −15 / 조건 불만 +10

### 적용 규칙 3가지 (`lib/signals.ts:294`)

1. 같은 신호가 두 번째부터 반복되면 `repeatEffects`를 쓴다 (예: 가격 부담 반복 시 저항 +25 → +15)
2. `maxTotal` 누적 상한 — 즉답 받음 24, 2분 내 응답 12. 성실한 응대가 반복된다고 신뢰가 무한히 쌓이지는 않는다
3. 신호마다 즉시 0~100 clamp — 바닥 친 축이 음수로 더 내려갔다가 뒤늦게 되살아나지 않는다

## 시간 신호는 시스템이 직접 계산

`lib/clock.ts`. LLM에 맡기면 시계 계산을 틀리므로 타임스탬프에서 직접 읽는다.

- 2분 내 응답 engagement +4 (상한 12) / 상담사 3분+ 지연 trust −10
- **고객 무응답** — 10분 −10, 30분 −20·저항 +10, 3시간 −35·intent −10, 8시간 −50·intent −8. 넘긴 단계는 **전부** 중복 적용된다
- **상담사 무응답** — 5분 trust −10, 15분 intent −15·engagement −35·trust −15. 이 둘은 "지금 이어지고 있는" 침묵에만 붙어서, 상담사가 답하는 순간 재계산에서 빠지고 온도가 그만큼 되살아난다. 늦게라도 답했다는 흉터는 3분 지연 신호가 따로 남긴다
- 고객 무응답도 같은 원리로 "지금 이어지는 침묵"만 센다 — 중간에 끊겼다가 다시 답한 구간은 대화로 회복된 것으로 본다
- 모든 경과 시간은 22:00~08:00 야간을 뺀 실 경과분 (`lib/time.ts:20`). 예: 13:44 → 익일 09:10 은 19시간 26분이 아니라 9시간 26분

## 가드 2개 (`app/api/temperature/route.ts`)

- **환각 가드** — 근거 발화(quote)가 실제 대화에 없는 신호는 점수에 넣기 전에 버린다. 폐기 수가 LLM 신호의 절반 이상이면 `confidence: "low"`로 낮춘다
- **길이 페널티 예외** — "넵 바로 할게요"처럼 짧아도 긍정 신호가 실린 발화는 `engage.shorter_reply`(길이 감소 −12)에서 뺀다

## 구간 (`lib/score.ts:40`)

| 온도 | 밴드 | surfacing |
|---|---|---|
| 70–100 | 🔥 대화가 잘 통해요 | none |
| 55–69 | 🙂 분위기 좋아요 | none |
| 40–54 | 🌡️ 살짝 식었어요 | counselor |
| 25–39 | 🥶 차가워요 | card |
| 0–24 | 🧊 거의 떠남 | team |

surfacing은 화면에 무엇을 띄울지의 구간 기준일 뿐, 실제 개입 카드 발화 여부는 아래 T/B 규칙이 최종 결정한다.

## 개입 트리거 (`lib/intervention.ts`)

온도 절대값만 보면 늦으므로 **속도**까지 본다.

- **T1** 온도 ≤ 39 · **T2** 한 턴 하락 ≥ 8 · **T3** 3턴 하락 합 ≥ 12 — 하나라도 걸리면 L2 카드
- 미달이어도 한 턴 −4 / 3턴 −6부터는 L1 힌트(문구만 회색 한 줄)
- 차단 규칙: **B1** 직전 3턴 내 개입함(카드 → 힌트 강등) · **B2** 명시적 거절 이후(처방 불가, 정중 종료만) · **B3** 상담사가 이미 그 축의 처방 방향으로 대응 중 · **B4** 2턴 연속 상승 · **B5** 하락의 과반이 상담사 귀책(응답 지연·설명 반복 → 고객 카드 대신 운영 알림)
- 처방 축은 "가장 많이 나빠진 축"이 정한다. 동점이면 **trust > resistance > intent > engagement** — 신뢰 붕괴는 지금 되돌릴 수 있는 문제인데 여기서 engagement 처방(요약 + 종료 예고)이 나가면 살릴 수 있던 상담에 작별 인사를 보내게 된다
