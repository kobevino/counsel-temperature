# Anthropic API key (server-side only, never exposed to the client)
ANTHROPIC_API_KEY=

# Set to 1 to return deterministic mock temperature readings without calling the API
TEMPERATURE_MOCK=

# Set to 1 to use the keyword-template customer replies without calling the API
CUSTOMER_REPLY_MOCK=
