# OpenAI API key for the conversation temperature analysis (server-side only, never exposed to the client)
OPENAI_API_KEY=

# Set to 1 to return deterministic mock temperature readings without calling the API
TEMPERATURE_MOCK=
